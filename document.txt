============================================================
CHANGES LOG - Job Tracker Local Runner
============================================================

[2026-07-17 BATCH 6.3] ADD PHONE NUMBERS TO PROMPT
- Modified the simplified prompt strings in both detail.html and list.html to explicitly ask the AI agent to extract and return phone numbers in addition to verified emails.

[2026-07-17 BATCH 6.2] PROMPT SIMPLIFICATION — FOCUS ON VERIFIED EMAILS & VALID INTERN JOBS

PROBLEM SOLVED:
  - The multi-source prompts from BATCH 6.1 were too heavy and took too long for the agent to complete.
  - The user's main focus is strictly getting verified company employee emails and currently valid intern openings (not closed).

MODIFIED FILES:
  - templates/companies/detail.html — simplified p1 and p2 prompt strings
  - templates/companies/list.html   — simplified p1 and p2 prompt strings

CHANGES:
  Prompt 1 (Contact & Intern Job Research) is now lightweight:
    - Instructs the agent to find emails on website, LinkedIn, and Hunter.io.
    - Emphasizes verifying EVERY email on getprospect.com (VALID/DELIVERABLE only).
    - Instructs checking career page & LinkedIn for Intern/Fresher jobs.
    - Explicitly asks to confirm jobs are CURRENTLY OPEN and accepting applications.
    - Clean 3-part deliverable: Verified Emails, Open Intern Jobs, Failed/Unverified Emails.

  Prompt 2 (Email Pattern + Intelligence Scan) is now a fallback:
    - Instructs the agent to guess the email pattern if direct HR emails can't be found.
    - Uses Hunter.io/Apollo.io for the pattern and LinkedIn for HR/Recruiter names.
    - Generates likely emails and verifies them on getprospect.com.
    - Delivers the pattern used and the top 3 verified (or most likely) HR emails.

[2026-07-17 BATCH 6.1] PROMPT UPGRADE — MULTI-SOURCE CONTACT HUNTING

PROBLEM SOLVED:
  - Old prompts only searched the company website and LinkedIn.
  - Upgraded prompts now instruct the agent to search 10+ sources for contacts and emails.

MODIFIED FILES:
  - templates/companies/detail.html — upgraded p1 and p2 prompt strings + expanded Quick Links panel
  - templates/companies/list.html   — same upgrades for prompt-shortcut-btn handler + buildListPromptLinks function

CHANGES:
  Prompt 1 (Contact & Intern Job Research) now covers:
    - Company website (Contact, About, Team, Footer pages)
    - Hunter.io domain search
    - Apollo.io people search
    - RocketReach employee search
    - SignalHire HR search
    - ContactOut employee lookup
    - LinkedIn company page + LinkedIn Jobs
    - Wellfound / AngelList team contacts + job listings
    - Glassdoor job listings + reviews
    - Crunchbase founders and executive contacts
    - Google advanced search + Google dorks (site:linkedin.com/in)
    - GitHub org pages and public employee emails
    - Twitter/X company hiring posts
    - Press releases, blog posts, news articles
    - Indeed, Naukri, Internshala, Shine, Cutshort job boards
    Output format: Name | Role | Email | Phone | Source table + job listing table + failed emails

  Prompt 2 (Email Pattern + Intelligence Scan) now covers:
    Step 1 — Discover email pattern (Hunter.io + Apollo + RocketReach)
    Step 2 — Find real HR contacts (LinkedIn + Google dorks + Wellfound + Crunchbase)
    Step 3 — Verify all emails on GetProspect (only VALID/DELIVERABLE)
    Step 4 — Cross-check job listings (LinkedIn, Indeed, Naukri, Internshala, Cutshort, Wellfound)
    Step 5 — Re-verify and provide top 3 guessed emails if none found
    Output: HR contacts table + email pattern + open jobs + guessed emails fallback

  Quick Links panel now includes 8 links per company:
    Website, Careers, Hunter.io (with domain), Email Verifier, Apollo.io, RocketReach,
    SignalHire, Crunchbase, LinkedIn, Wellfound — all pre-populated with company name/domain

[2026-07-17 BATCH 6] AI PROMPT BUTTON + COPY LINK FOR COMPANIES

PROBLEM SOLVED:
  - Needed a fast way to generate AI agent prompts for each company (find HR contacts, verify emails, check intern jobs).
  - Needed a quick-copy button per company row to grab detail page URL without navigating.
  - Needed 2 pre-crafted, ready-to-paste prompts that dynamically inject the company name, website, and career URL.

MODIFIED FILES:
  - templates/companies/detail.html:
      - Added "🤖 Prompt" button in the Quick Actions sidebar card (btn-outline-success).
      - Added Prompt Modal (dark GitHub-style #0d1117 theme) with:
          * Quick Copy Links panel (Website, Careers, Email Verifier, Hunter.io) — each with a copy-to-clipboard button.
          * Prompt 1 — "Contact & Intern Job Research": visits company website, finds HR/employee emails/phones, verifies via getprospect.com, checks for open intern/fresher listings, double-verifies all.
          * Prompt 2 — "Deep LinkedIn & Career Page Scan": LinkedIn company search, career page scrape, cross-reference with Indeed, find HR contact, verify email, confirm all jobs are still open.
          * Individual Copy buttons per prompt + "Copy All Prompts" button.
      - All prompts are dynamically built with JS using company.website, company.name, company.career_page_url data attributes.

  - templates/companies/list.html:
      - Added 📋 (copy-link) button to each row's Actions column — copies the detail page URL to clipboard instantly with visual feedback.
      - Added 🤖 (robot) Prompt shortcut button to each row — opens identical dark Prompt Modal directly from the list view without navigating to the detail page.
      - Added listPromptModal HTML (same 2 prompts + quick links, separate IDs for list vs detail).
      - Added JS handlers: copy-link-btn (clipboard copy + green flash), prompt-shortcut-btn (modal open + prompt fill), listCopyPrompt1Btn, listCopyPrompt2Btn, listCopyAllPromptsBtn.

PROMPT CONTENT:
  Prompt 1 (blue badge):
    Go to [COMPANY WEBSITE] — find all HR/employee emails and phone numbers.
    Verify each email on https://getprospect.com/email-verifier (only deliver valid/deliverable).
    Check for open intern/fresher listings on career page and website.
    Verify each listing is still open and accepting applications.
    Double-check all details twice. Deliver structured summary.

  Prompt 2 (purple badge):
    LinkedIn company search — employee count, recent posts, intern/entry-level jobs.
    Career page + website scrape — all current open roles.
    Cross-reference LinkedIn Jobs + Indeed (last 30 days).
    Find HR manager email via LinkedIn, verify on getprospect.
    Confirm all listings still open. Return structured report, verified twice.

[2026-07-16 BATCH 5] JOBS PAGE FILTERS AND TODAY BADGES

PROBLEM SOLVED:
  - Jobs page needed visual indication for newly scraped jobs.
  - Removed (dismissed) jobs needed to be visually removed instantly on click and hidden by default.
  - Needed a quick way to filter jobs by status (Applied, Bookmarked, Rejected, All).

MODIFIED FILES:
  - blueprints/jobs.py : Added status_filter logic and passed today_str for template comparison. Hidden dismissed jobs by default unless filtering by All/Rejected.
  - templates/jobs.html: Added a "NEW TODAY" badge with pulse animation for jobs scraped today. Added interactive filter chips for all statuses (Default, New, Bookmarked, Applied, Rejected, All). Modified setStatus JS function to instantly hide a dismissed card with an animation if the current view isn't 'All' or 'Rejected'.

[2026-07-16 BATCH 4] JOBS PAGE FILTERS AND DATE FIXES

PROBLEM SOLVED:
  - Jobs page had no easy way to filter for Internships or specific company types.
  - The date displayed for jobs was always today's date (the scraped date), instead of the actual posted date.

MODIFIED FILES:
  - blueprints/jobs.py : Added `intern` and `type` (startup/mnc/it) query parameters to the `index` route. Modified the database query to filter `ScrapedJob` based on these parameters. Added counts for internships and startups to pass to the template.
  - templates/jobs.html: 
      - Added interactive "Filter Chips" (All Jobs, Internships, Startups, MNC / Corporate) below the search bar.
      - Fixed date display logic: now shows `posted_date` (if available) with a calendar icon, and `scraped_at` with a clock icon and "Scraped" label.
      - Updated pagination links to preserve the selected filter state.

[2026-07-16 BATCH 3] COMPANY CAREER PAGES IN DEEP SEARCH

PROBLEM SOLVED:
  - Deep Search was only scraping job portals, ignoring the user's saved companies list
  - jobs.html 500 error: url_for('aggregator.index') was wrong (2 places fixed)

MODIFIED FILES:
  - services/deep_scraper.py : Added Phase 2 — after all portals are scraped, the scraper
                               loops through all Company records with career_page_url set,
                               calls scrape_career_page() on each, and saves ATS-matched
                               results to ScrapedJob table. Progress bar total updates live.
  - templates/search.html    : Progress bar now shows phase label ("Phase 1: Job Portals" /
                               "Phase 2: Your Companies"). Log lines colour-coded: yellow for
                               phase headers, orange/yellow for warnings, bold for completion.
  - templates/jobs.html      : Fixed 2nd aggregator.index → aggregator.search_jobs_page

HOW PHASE 2 WORKS:
  - Queries Company.query.filter(career_page_url != NULL)
  - Calls scrape_career_page(url) for each company
  - Enriches job dict with company.name and company.company_type
  - Sets source = "{CompanyName} (Career Page)"  so you can filter by it in Jobs page
  - Saves to ScrapedJob (NOT Opening/CRM) — companies list stays clean
  - Scrapes concurrently/sequentially without the 3-10s random evasion delay used in Phase 1 
    (since these are all distinct domains, no IP ban risk from aggressive polling).

[2026-07-16 BATCH 2] BLOCKED SCRAPERS FIX + SCRAPPEDJOB TABLE + PREMIUM JOBS UI

PROBLEM SOLVED:
  - Wellfound, Glassdoor, Shine, Cutshort were returning 0 jobs (blocked/wrong selectors)
  - Scraped jobs were being added to the Company CRM list (wrong table)
  - Jobs page had no UI to show deep-scraped jobs with full details

MODIFIED FILES:
  - models.py                      : Added ScrapedJob model (standalone table, no FK to Company)
  - blueprints/jobs.py             : Rewritten to query ScrapedJob table with search/filter/sort/status
  - templates/jobs.html            : Premium card-grid UI with ATS bar, source badges, company type labels,
                                     Bookmark/Applied/Dismiss buttons per card
  - services/deep_scraper.py       : Switched DB save from Company+Opening to ScrapedJob table
  - services/wellfound_scraper.py  : Rewritten with better stealth headers, Firefox prefs, image blocking
  - services/glassdoor_scraper.py  : Rewritten to use Glassdoor GraphQL API (bypasses Cloudflare)
  - services/shine_scraper.py      : Rewritten to use httpx instead of Playwright (faster, lighter)
  - services/cutshort_scraper.py   : Rewritten to use Cutshort public search REST API
  - templates/search.html          : Post-deep-scrape redirect now goes to /jobs/ instead of /

SCRAPED JOBS UI FEATURES:
  - Card grid layout (responsive, 340px+ columns)
  - Colour-coded top strip: green = high ATS, yellow = mid, grey = low
  - Source badge: LinkedIn (blue), Naukri (green), Indeed (red), etc.
  - Company Type badge: Startup (yellow), MNC (purple), IT/Tech (green)
  - Internship badge (auto-detected from title)
  - ATS score bar (visual % fill)
  - Posted date + scraped date shown
  - Apply button (opens URL in new tab)
  - Bookmark / Applied / Dismiss buttons (AJAX, no page reload)
  - Sort by Recent or Best Match
  - Filter by platform (dropdown)
  - Live search with debounce

[2026-07-16 BATCH 1] DEEP SCRAPE ARCHITECTURE - TIME-GAPPED MULTI-PLATFORM

CREATED FILES (Batch 1):
  - services/deep_scraper.py        : Background thread queue. Scrapes 14 platforms
                                      with random 3-10s evasion delays between each.
  - services/simplyhired_scraper.py : SimplyHired scraper using Playwright/Firefox
  - services/remoteok_scraper.py    : RemoteOK scraper using their public JSON API
  - services/technopark_scraper.py  : Technopark (Trivandrum) scraper using Playwright
  - services/freshersworld_scraper.py: Freshersworld scraper using Playwright
  - services/hirist_scraper.py      : Hirist.tech scraper using Playwright
  - services/instahyre_scraper.py   : Instahyre scraper using Playwright

BUGS FIXED (Batch 1):
  - foundit_scraper.py: NameError href and date_elem were undefined
  - ImportError: Job model → switched to Company+Opening (then later to ScrapedJob)
  - ImportError: app module → pass current_app via aggregator.py
  - ImportError: scheduler → replaced with threading.Thread

============================================================

SESSION 6 (Prompt Engineering)
-----------------------
- Rewrote user's agent prompt for faster execution, focusing strictly on finding verified internship contacts (HR/Managers) and removing slow steps like scraping career pages or listing failed emails.
- FIXED: list.html phone number editing (added inline edit UI directly on the list page)
- FIXED: Javascript syntax error in detail.html preventing the save phone and deep scan buttons from working

SESSION 7 (Question Answered & Branch Auto-Switching)
-----------------------
- Answered a user query about which branch code runs when executing `start.bat`.
- MODIFIED: start.bat to automatically save the current branch, checkout `main` before running the Python script, and checkout the saved original branch when the script exits.
